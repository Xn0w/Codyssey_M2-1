# ADAIR 변경 요청 회신 — UAV 담당

| 항목 | 내용 |
|---|---|
| 대상 요청서 | ADAIR 담당자별 변경 요청서 (Orchestrator v0.1.2 연동 준비, 2026-09-23) |
| 회신일 | 2026-09-24 |
| 회신자 | UAV 담당 (김동현) |
| 회신 범위 | **R05** (UAV 주관), **R01** (원본 응답 형식 협조) |
| 기준 | 8bfe971 이후 UAV 로컬 작업본. **아직 GitHub 미반영** (아래 "반영 커밋" 참조) |

R02·R03·R04·R06은 UAV 담당 범위가 아니므로 회신하지 않습니다. 다만 R03·R06과 맞닿은 UAV 측 제약은 5절에 적었습니다.

---

## 0. 먼저 알려드릴 변경

| 변경 | 내용 |
|---|---|
| 경로 | `px4/uav-agent/` → **`uav/uav-agent/`** 로 이동. 요청서의 코드 근거 경로도 `uav/`로 읽어 주세요. |
| SDK | MAVSDK 3.x(gRPC) → **4.x(네이티브 바인딩)** 로 전환. 8bfe971의 `drone.py`는 3.x 코드입니다. |
| 문서 | 실행 방법 [uav/README.md](../../uav/README.md), API 사양 [uav/API_DEFINE.md](../../uav/API_DEFINE.md) 신설 |
| Gazebo | 강원(원통–기린) 지형 월드 추가 — `uav/gazebo/kangwon.sdf`, `uav/px4-start-kangwon.sh`. 월드 원점은 `gz_bridge.py` datum과 동일 |

---

## 1. R05 — UAV 단위와 상태 출처 확인

### 1-1. SDK 버전

| 항목 | 값 |
|---|---|
| Python | 3.14.4 |
| MAVSDK | `mavsdk` **4.0.0** (네이티브 바인딩, `requirements.txt`: `mavsdk>=4,<5`) |
| FastAPI / pydantic / uvicorn | 0.141.1 / 2.13.5 / 0.53.0 |
| PX4 | Docker `px4io/px4-sitl-gazebo:latest` (Gazebo Harmonic) |

3.x와 4.x는 API가 호환되지 않습니다. mavsdk 3.x 환경에서 real 모드를 띄우면 동작하지 않습니다.

### 1-2. 배터리 단위 — **0~100 (%)**

| 단계 | 값 | 비고 |
|---|---|---|
| PX4 원값 | 0~1 | MAVLink/PX4 내부 표현 |
| MAVSDK `remaining_percent` | **0~100** | SDK가 ×100 하여 제공. 2026-09-15 SITL 연결 실측에서 만충 시 `100.0` 확인 |
| UAV API `battery.percent` | **0~100** | `Px4Drone`이 변환 없이 그대로 전달 |
| evaluator / Mock | % 기준 | Mock 기본 95.0 |

- **변환 책임**: 원본 제공부(UAV)에서 이미 % 단위이므로 **UAV·통합 모두 추가 변환 불필요**합니다.
- 09-15 실측은 3.x 환경에서 한 것입니다. 4.x 환경의 원값·API 값 대조 로그는 real 서버 기동 시 함께 첨부하겠습니다.
- 신뢰도 주의: SITL 방전값이 100→95→88→93→100으로 되돌아오는 현상이 관찰됐습니다(포트 경합 상태 측정이라 재측정 필요). 소모율 `BATTERY_DRAIN_PCT_S = 0.033 %/s`는 추정치입니다.

### 1-3. endpoint와 모드

| resource_id | endpoint | 모드 | 상태 |
|---|---|---|---|
| A-uav1 | `http://3.37.210.229:8000` | real (PX4 SITL) | **평소 꺼둠.** 필요 시 UAV 담당에게 요청 |
| A-uav2, B-uav1, B-uav2 | 각자 로컬 (`http://localhost:800x`) | mock | 서버 배포 없음 |

- 비용 문제로 서버는 상시 운영하지 않습니다. 테스트는 로컬 mock으로 해 주세요 (`uav/README.md` 2번).
- 서버 모드는 `GET /health`의 `mode`, `uav_id`로 확인합니다.
- UAV 1대 = 프로세스 1개 = 포트 1개입니다. 경로의 `uav_id`가 서버 담당과 다르면 404를 반환합니다.
- ⚠️ 현재 IP는 고정(Elastic) IP가 아니어서 서버를 재기동하면 바뀔 수 있습니다. 바뀌면 기동 요청 시 안내하겠습니다.

### 1-4. 고정값·파생값 필드 — **총괄 가용성 근거로 사용 금지**

| 필드 | real 모드 실제 동작 |
|---|---|
| `health.failsafe` | 항상 `false` — PX4 health telemetry에 failsafe 항목 없음 |
| `link_quality` | 항상 `"OK"` |
| `current_task_id` | 항상 `null` — **mock에서도 execute 중 갱신 안 됨** |
| `wind_ms` | 항상 `null` — 현재 PX4 SITL 구성에서 wind telemetry 미발행(실측) |
| `timestamp` | UAV 서버의 응답 생성 시각(ISO8601 UTC). PX4 텔레메트리 시각 아님 |
| `velocity_ms` | 수평 속도만 (N·E 성분) |
| `sensors_ok` | 자이로·가속도계·지자기계 캘리브레이션 플래그 AND |
| `position.alt_m_agl` | 이륙 지점 기준 상대고도. 실제 지면 기준 AGL 아님 |

따라서 Connector의 `state = OFFLINE if failsafe else READY`는 real에서 **항상 READY**입니다. 이는 Adapter 파생값이며 UAV가 확인한 가용성이 아닙니다.

### 1-5. mock과 real의 차이

요청에 `wind_ms`가 없으면 **mock은 내부값(3.1 m/s)으로 판단**하고, **real은 `REJECT / WIND_UNKNOWN`** 을 반환합니다. 통합에서는 항상 `wind_ms`를 실어 보내 주세요.

---

## 2. R01 — UAV 원본 응답 형식 (협조)

UAV 측 응답 스키마는 변경하지 않습니다(`uav/uav-agent/models.py`). 총괄 Adapter는 아래 형식을 그대로 보존하면 됩니다. 전체 사양은 [API_DEFINE.md](../../uav/API_DEFINE.md)에 있습니다.

### 2-1. 보존 대상 필드

| 응답 | 필드 |
|---|---|
| `/state` | `uav_id`, `timestamp`, `position{lat,lon,alt_m_amsl,alt_m_agl}`, `battery{percent,voltage}`, `velocity_ms`, `flight_mode`, `armed`, `health{gps_ok,sensors_ok,failsafe}`, `wind_ms`, `link_quality`, `current_task_id` |
| `/evaluate` | `task_id`, `decision_id`, `uav_id`, `verdict`, `eta_sec`, `reason`, `detail`, `counter_offer`, `constraints` |

- `task_id`, `decision_id`는 요청값을 그대로 echo합니다.
- **`eta_sec`**: ACCEPT·COUNTER는 정수(초)이고, 목표가 현재 위치와 같으면 **0이 나올 수 있습니다**. REJECT는 `null`입니다. 단 `LOW_BATTERY`는 `constraints.eta_sec`에 계산값이 있습니다.
- **`detail`**: 일부 REJECT에만 있고 나머지는 `null`입니다.
- **reason 코드**: 모두 `interfaces/schema.py`의 `RejectReason`에 등록되어 있습니다 (`FAILSAFE_ACTIVE`, `BUSY`, `WIND_UNKNOWN`, `MODERATE_WIND`, `DEADLINE_TIGHT` 포함).

### 2-2. MODERATE_WIND 판정 순서 — 총괄 정책 확인 요청

판정 순서는 다음과 같습니다.

```
WIND_UNKNOWN → FAILSAFE_ACTIVE → SENSOR_FAILURE → COMMUNICATION_FAILURE → BUSY
→ REQUIRED_CAPABILITY_UNAVAILABLE → HIGH_WIND(≥10) → LOW_BATTERY
→ RETURN_MARGIN_INSUFFICIENT(COUNTER) → TIMEOUT → DEADLINE_TIGHT(COUNTER)
→ MODERATE_WIND(COUNTER, 8≤wind<10) → ACCEPT
```

**풍속이 8~10 m/s여도 앞의 COUNTER 사유에 먼저 걸리면 `reason`은 그 사유로 나옵니다.** 이때도 풍속은 `constraints.wind_ms`에 남습니다. 총괄이 `reason == "MODERATE_WIND"`만 정책 수용한다면 이 경우는 수용되지 않습니다. 의도하신 동작인지 확인 부탁드립니다.

### 2-3. 응답 예시 (mock, 2026-09-24 실행)

조건: A-uav1 원통119(38.1205, 128.2018, 235 m) → 목표(38.0425, 128.2541, 지면 804 m)

```json
// GET /uav/A-uav1/state
{"uav_id":"A-uav1","timestamp":"2026-09-24T07:01:05Z",
 "position":{"lat":38.1205,"lon":128.2018,"alt_m_amsl":235.0,"alt_m_agl":0.0},
 "battery":{"percent":95.0,"voltage":15.8},"velocity_ms":0.0,"flight_mode":"HOLD","armed":false,
 "health":{"gps_ok":true,"sensors_ok":true,"failsafe":false},
 "wind_ms":3.1,"link_quality":"OK","current_task_id":null}
```

| 조건 | 결과 |
|---|---|
| wind 3.1, 배터리 95 | `ACCEPT`, `eta_sec: 860`, `constraints.return_margin_pct: 36.3` |
| wind 9.0, 배터리 95 | `COUNTER / MODERATE_WIND`, `eta_sec: 860`, `counter_offer: {"note":"풍속 주의. 고도 하향 권장"}` |
| wind 9.0, 배터리 78.5 | `COUNTER / RETURN_MARGIN_INSUFFICIENT` (**MODERATE_WIND 아님**), `counter_offer: {"observe_duration_s":30,...}` |
| wind 12.4 | `REJECT / HIGH_WIND`, `eta_sec: null`, `detail: "wind 12.4 m/s exceeds limit 10.0 m/s"` |
| wind 3.1, 배터리 20 | `REJECT / LOW_BATTERY`, `eta_sec: null`, `constraints.eta_sec: 860` |

```json
// ACCEPT 전체 응답
{"task_id":"task-101","decision_id":"dec-001","uav_id":"A-uav1","verdict":"ACCEPT","eta_sec":860,
 "reason":null,"detail":null,"counter_offer":null,
 "constraints":{"distance_km":9.81,"battery_now_pct":95,"battery_after_pct":36.3,"return_margin_pct":36.3,
                "wind_ms":3.1,"required_climb_m":619,"eta_sec":860}}
```

---

## 3. 호출 예시

```bash
# 상태
curl http://<endpoint>/uav/A-uav1/state

# 평가 (wind_ms, target.alt_m_amsl 필수로 넣을 것)
curl -X POST http://<endpoint>/uav/A-uav1/evaluate -H 'Content-Type: application/json' -d '{
  "task_id":"task-101","decision_id":"dec-001",
  "target":{"lat":38.0425,"lon":128.2541,"alt_m_amsl":761},
  "observation_type":"THERMAL","wind_ms":3.1}'

# 실행 (Safety ALLOW 이후에만)
curl -X POST http://<endpoint>/uav/A-uav1/execute -H 'Content-Type: application/json' -d '{
  "task_id":"task-101","decision_id":"dec-001",
  "target":{"lat":38.0425,"lon":128.2541,"alt_m_amsl":761},"observation_type":"THERMAL"}'
curl http://<endpoint>/uav/A-uav1/task/task-101
```

---

## 4. 검증 결과

| 항목 | 결과 |
|---|---|
| mock `/state`, `/evaluate` 응답 형식 (ACCEPT 1, COUNTER 2, REJECT 2) | ✅ 확인 (2026-09-24) |
| 배터리 단위 0~100 | ✅ 3.x 환경 실측 (2026-09-15). 4.x 대조 로그는 미첨부 |
| real endpoint `/state`, `/evaluate` 응답 | ⏳ **미확인.** 서버 기동 시 확보해 추가 공유 |
| 강원 지형 월드 재생성 결과가 원본과 동일 | ✅ 확인. EC2 headless 부하 검증은 미실시 |

---

## 5. 남은 제약 (UAV 측)

| # | 제약 | 영향 |
|---|---|---|
| 1 | **BUSY 미동작** — execute 중에도 `current_task_id`가 갱신되지 않아 같은 기체를 재평가하면 ACCEPT | 총괄이 실행 중 기체를 별도로 제외해야 함. UAV 측 수정 예정 |
| 2 | **execute `COMPLETED` ≠ 도착·관측 완료** — 이동 명령 접수 2초 후 COMPLETED, 관측값(`max_temp_c: 312.5`)은 고정값 | 화재 관측 완료로 간주하지 말 것 |
| 3 | **착륙·복귀·중단 API 없음** — 실행 명령은 execute(시동→이륙→goto) 하나뿐 | 합의안의 "실행 중 REJECT 시 복귀" 미구현 |
| 4 | **고도 기준 불일치** — 합의안은 AGL + terrain-following, 코드는 목표 지면 AMSL + 50 m, 경로 중간 지형 미고려 | 강원 지형 월드에서는 능선(최고 약 1,470 m) 충돌 가능 |
| 5 | reason 코드 명칭이 합의안(`docs/uav/uav_final`)과 다름 — 합의안 `INSUFFICIENT_BATTERY`·`DEADLINE_INFEASIBLE` vs 코드·schema `LOW_BATTERY`·`TIMEOUT` | 팀 기준 확정 필요. 현재는 schema 기준 |
| 6 | 임계값(풍속 8/10 m/s, 복귀 여유 20%)과 소모율은 잠정·추정값 | 실측 후 조정 |
| 7 | Task 상태는 메모리 보관 — 서버 재시작 시 소실 | |

## 6. 통합 담당께 요청 (UAV 측에서 필요한 것)

1. **`target.alt_m_amsl`에 목표 지점 지면 해발고도를 넣어 주세요.** `Assignment.target_alt_m` 기본값 0.0이 그대로 가면 상승량이 0으로 계산되어 ETA·배터리 판단이 틀립니다. 예: 목표 지점은 팀 DEM(`dem_gangwon.tif`) 기준 761 m.
2. **`wind_ms`는 항상 실어 보내 주세요** (1-5 참조).
3. 1-4의 고정값 필드를 READY/OFFLINE 판단 근거로 쓰지 말아 주세요.
4. 코드 주석의 `px4/` 경로와 `SHARE.md` 참조를 갱신해 주세요 — `config.py:55,65`, `interfaces/schema.py:127,190`, `connectors/uav_connector.py:6,8,121`.
5. `config.UAV_ENDPOINTS`의 `A-uav1`은 서버 기동 시 안내하는 주소로, 나머지는 로컬 mock 주소로 설정해 주세요.

---

## 7. 반영 커밋

**미반영.** 로컬 변경 내용은 다음과 같습니다.

- `px4/` → `uav/` 경로 이동
- `drone.py` MAVSDK 4.x 전환, `requirements.txt` UAV 의존성 추가
- `uav/README.md`, `uav/API_DEFINE.md` 신설, `uav/NOTE.md`·`uav/uav-agent/README.md` 현행화
- `uav/gazebo/` 강원 지형 월드, `uav/px4-start-kangwon.sh` 추가
- 본 회신 문서

커밋 해시: **(push 후 기입)**
