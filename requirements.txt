# 산불 대응 멀티에이전트 시뮬레이터 — 공통 의존성
# 파트별로 사용하는 패키지를 추가해 주세요.

# --- 지상자원(UGV/소방차) ---
# 주의: 'mavsdk' 패키지가 'mavsdk-grpc'로 이름이 변경되었다.
# 기존 'mavsdk' 이름은 이제 다른 API(네이티브 바인딩)를 가리키므로
# 'from mavsdk import System' 은 더 이상 동작하지 않는다.
# 임포트는 'from mavsdk_grpc import System' 형태로 사용할 것.
mavsdk-grpc

# --- UAV (uav-agent) ---
# UAV 는 네이티브 바인딩 'mavsdk' 4.x 를 쓴다 (import mavsdk). 위 mavsdk-grpc 와 import 이름이 달라 공존 가능.
# 4.x 의 battery.remaining_percent 는 0~100 (PX4 원값 0~1 을 ×100).
mavsdk>=4,<5
fastapi
uvicorn
pydantic

# --- 공통 ---
pytest